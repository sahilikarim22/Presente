<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error</title>
    <!-- Add your project-specific stylesheets here -->
    <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/main.css">
    <!-- Add any other stylesheets your project uses -->
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f8f9fa; /* Use your project's background color */
            color: #343a40; /* Use your project's text color */
            text-align: center;
            margin: 0;
            display: flex;
            flex-direction: column;
            justify-content: center;
            height: 100vh;
        }

        h1 {
            color: #dc3545; /* Use your project's error color */
        }

        p {
            font-size: 18px;
            margin-bottom: 20px;
        }

        a {
            display: inline-block;
            padding: 10px 20px;
            background-color: #007bff; /* Use your project's button color */
            color: #fff; /* Use your project's button text color */
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        a:hover {
            background-color: #0056b3; /* Use your project's button hover color */
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>Error</h1>
        <p><%= errorMessage %></p>
        <a href="javascript:history.back()">Volver a la página anterior</a>
    </div>
</body>

</html>
